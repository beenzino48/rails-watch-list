<div class="container">
<nav class="navbar navbar-light bg-light px-3">
  <a class="navbar-brand" href="#">Stream movies online for free!🏴‍☠️</a>
      <li class="nav-item active">
        <%= link_to "Home", lists_path(@list), class: "btn bg-light" %>
                <%= link_to "New list", new_list_path(@list), class: "btn bg-light" %>
      </li>
</nav>

<div class="banner" style="background-image">
<h1>Bookmarks all the films you would like to watch "legally"</h1>
<p></p>
</div>

  <h1 class="text-center my-5 text-white"></h1>
    <div class="row">
    <% @lists.each do |list| %>
      <div class="col-6 col-lg-4">
        <div class="card mb-3 bg-light">
        <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path list.photo.key, height: 300, crop: :fill %>')">
</div>
          <h1><%= list.name %></h1>
          <%= link_to "More details", list_path(list), class: "btn bg-secondary" %>
        </div>
      </div>
    <% end %>
    </div>
</div>
